Pre-processing step

1. netflix id -> imdb id (via imdb search by movie on OS)
2. imdb id -> deduped imdb id
3. deduped imdb id -> (subs ids, subs link(s))
4. download [(subs ids, subs link(s))]
5. deduped imdb id -> plot(s)/synopsis
6. extract from netflix useful ratings

Processing step

1. build features
2. VSM
